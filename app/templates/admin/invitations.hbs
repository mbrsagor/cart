<h1>Invitations</h1>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>E-mail</th>
        </tr>
    </thead>
    <tbody>
        {{#each model as |invitations|}}
        <tr>
            <th>{{invitations.id}}</th>
            <td>{{invitations.email}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>
